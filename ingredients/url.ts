export const URLs: Record<string, string> = { // Record 잘 기억하기
    "local": process.env.NEXT_PUBLIC_API_URL || 'http://127.0.0.1:8000',
    "movies": "https://nomad-movies.nomadcoders.workers.dev/movies",
}

